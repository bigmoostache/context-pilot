**/target
**/__pycache__
.env
# Per-unit provisioning secrets — the admin seed password (M6). NEVER commit.
seed.env
**/seed.env
# Provider API keys: the rendered env (plaintext) is box-only; never commit it.
# Per-client vaults (group_vars/<client>/vault.yml) ARE committed, but only
# ansible-vault-ENCRYPTED — never a plaintext one.
providers.env
**/providers.env
# Generated per-unit delivery sheets (printable email/password).
deploy/ansible/out/
# Ansible-built appliance artifacts (downloaded caddy, etc.).
deploy/ansible/.artifacts/
/.context-pilot/*
snake
tldr_debug.log
issues.md
test
WORKING_CONFIG_CAPTURED.md
CLAUDE_CODE_UPDATE_2.1.44.md_lorem.txt
.context-pilot-backup
.claude/
test.md
report/code-audit.md
test
output
!.context-pilot/shared/
.context-pilot/shared/entities/
!.context-pilot/agents/
!.context-pilot/skills/
!.context-pilot/commands/
test-typst
brilliant-cv/cv.pdf
brilliant-cv/cv.typ
brilliant-cv/metadata.toml
brilliant-cv/modules_en/education.typ
brilliant-cv/modules_en/professional.typ
brilliant-cv/modules_en/projects.typ
brilliant-cv/modules_en/skills.typ
brilliant-cv/modules_fr/certificates.typ
brilliant-cv/modules_fr/education.typ
brilliant-cv/modules_fr/professional.typ
brilliant-cv/modules_fr/projects.typ
brilliant-cv/modules_fr/publications.typ
brilliant-cv/modules_fr/skills.typ
brilliant-cv/modules_zh/certificates.typ
brilliant-cv/modules_zh/education.typ
brilliant-cv/modules_zh/professional.typ
brilliant-cv/modules_zh/projects.typ
brilliant-cv/modules_zh/publications.typ
brilliant-cv/modules_zh/skills.typ
graceful-genetics/a-mail.png
graceful-genetics/main.typ
check_sender
increment.txt
workdir/
increment.txt
tmp/freeze_test_2.txt
tmp/freeze_test.txt
tmp/test_queue.txt
docs/slides/node_modules/
docs/slides/package-lock.json
docs/slides/package.json
docs/slides/slides.pdf
**/.DS_Store

# mitmproxy / API capture artifacts (huge, never commit)
requests.log
requests.flow
**/requests.log
**/requests.flow
/tmp/cc-capture/

# Harbor / terminal-bench run outputs
/jobs/
benchmarks/terminal-bench/jobs/
/sandbox/

# Bridge runtime artifacts (written into the agent folder = project root when
# the TUI runs with CP_BRIDGE=1). Never commit — they are per-run, per-machine.
/oplog/
/heartbeat
/bridge.lock
/stream.sock
demo/
.cp-trash/flame.svg
.cp-trash/ideas.md
.cp-trash/tedx.md
.cp-trash/test-ocr.pdf
.cp-trash/wiring-alignment-audit.md
docs/test.xlsx
sandbox/
/.uploads
*.sourced_pdf
# Playwright MCP browser session dumps (generated per run, never commit)
.playwright-mcp/

# Stray npm lockfile — the web/ project uses pnpm (pnpm-lock.yaml)
web/package-lock.json

devis-attali-context-pilot.md

# Python virtualenv (local Ansible control node)
.venv/

# Ansible working inventory (real client hosts — keep only the .example tracked)
deploy/ansible/inventory.ini

# Ansible runtime secrets (per-client, passed with -e @file — never committed)
deploy/ansible/*.local.yml
